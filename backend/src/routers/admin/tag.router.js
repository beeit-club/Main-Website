// // Lấy danh sách tags
// router.get('/tags', postController.getTags);

// // Lấy tag theo ID
// router.get('/tags/:id', postController.getTagById);

// // Tạo tag mới
// router.post('/tags', authenticateToken, postController.createTag);

// // Cập nhật tag
// router.put('/tags/:id', authenticateToken, postController.updateTag);

// // Xóa tag (soft delete)
// router.delete('/tags/:id', authenticateToken, postController.deleteTag);

// // Khôi phục tag
// router.patch('/tags/:id/restore', authenticateToken, postController.restoreTag);
